export interface Alternatives {
  title: string;
  image?: string;
  price: number;
  unit: string;
}

export interface ShareData {
  files?: File[];
  text?: string;
  title?: string;
  url?: string;
}
